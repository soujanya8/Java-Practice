public class Mathfunctions {
    public static void main(String[] args){
        //Math is a class and it has so many math functions as methods
        //Lets see some examples
        double result;
        //result = Math.PI; it returns value of PI
        //result = Math.pow(2 , 5); 2^5 
        //result = Math.abs(-6); converts negavtive to positive
        //result = Math.sqrt(16);
        //result = Math.round(3.15); rounds the decimal to the nearest whole number
        //result = Math.ceil(3.15); it rounds to the next whole number
        //result = Math.floor(3.99); it rounds to the previous whole number that is 3
        //result = Math.max(13,15);
        result = Math.min(13,15);

        System.out.println(result);
    }
    
}
